# Long-term Memory

## Recurring Daily Tasks

- On 2026-03-27, user disabled the previously assumed 7709 / related recurring push workflow. This cancellation should be interpreted narrowly: it turns off the 7709-style recurring report, not the separate valuation-broadcast workflows unless the user explicitly says so.
- User reaffirmed on 2026-03-27 that the 7709 recurring push must be fully shut down and should not surface again unless the user explicitly re-enables it; accidental reappearance is considered especially annoying and should be treated as a preventable error.
- Active recurring task: send a finance/news monitoring morning report every day at 07:30 Beijing time. This schedule includes non-trading days too. Monitoring window is fixed as the previous calendar day 15:00 through the current day 07:20, with ranking priority given to genuinely new previous-afternoon-through-overnight items and same-morning incremental heat rather than stale prior-day leftovers. Default delivery mode is the standard version; only switch to the strengthened version when the user explicitly asks.
- Active recurring task (Task 1): on trading days, send the 09:45 Beijing-time valuation broadcast, then resend the same content at 10:00 as a reminder.
- Active recurring task (Task 2): on trading days, send the 14:30 Beijing-time afternoon valuation broadcast, then resend the same content at 14:40 as a reminder.
- User reaffirmed on 2026-03-27 that these three recurring workflows (08:30 morning finance/news report, Task 1, and Task 2) must remain enabled unless the user explicitly changes them.
- On 2026-03-28, a preventable scheduling mistake was confirmed: the 08:30 morning report had been remembered as active but had not actually been created as a cron job, while old 7709 cron jobs were still enabled. Future rule: for recurring workflows, do not trust memory or prior chat alone; verify real cron state in `~/.openclaw/cron/jobs.json` or `openclaw cron list`, then reconcile config so enabled/disabled reality matches the agreed schedule.
- On 2026-03-28, the cron state was corrected: disabled old 7709 cron jobs and created the five intended cron jobs for 08:30 daily morning report, 09:45 + 10:00 Task 1, and 14:30 + 14:40 Task 2.

## Workflow shortcuts
- If the user sends a recording and says `plan a`, first verify that the media landed locally. If it did, automatically run the A3 enhanced OCR workflow and output the standardized report with simulated bullish comments and representative original quotes.

## Reporting preferences
- On 2026-04-03, user requested that all successful "最强写手" outputs be continuously recorded into a running style library / memory for ongoing learning. The target style is humorous, ironic, high-rhythm short-video scripting optimized for 豆包, preserving joke structure and punchy segment cadence.
- On 2026-03-31, user clarified a delivery preference: whenever sending HTML files through chat, default to a directly openable public external-link version (like a temporary tunnel URL), not just local workspace paths or attached local filenames.
- On 2026-04-15, the user made a long-term website-maintenance rule explicit: for main-site pages such as `pool-state`, `daily-signal`, `morning-report`, homepage summaries, and strategy overview pages, important presentation rules must be implemented in the underlying generation logic or durable source-of-truth files, not only by hand-editing the published HTML. Otherwise regressions after unrelated updates are considered a preventable error. These durable rules include: de-brand product names by default (remove `南方东英` / `南方`), preserve client-facing wording, keep `pool-state` header minimal (time range + calculation logic), keep the `境内A股账户可投资产品` section present and prioritized where expected, and prefer neutral headings like `今日关注建议` / `今日一句话总结` over salesy phrasing.
- For finance/news monitoring reports, do not expose internal reasoning, methodology, or write-up process inside the formal report body.
- On mainland China platforms such as 东方财富、雪球、同花顺 and similar 境内平台, default mapping must stop at ETF / 指数 / 个股 / 板块. Do not directly map those platforms' discussion heat to leveraged products unless the user explicitly asks for a special exception.
- The desired morning report case/template is now considered clear and locked in: use the 7-part structure already established, keep the writing as a user-facing finished report rather than analyst notes, and apply the mainland-platform ETF/index/stock/sector-only rule consistently in sections about platform heat and product mapping.
- Refinement on 2026-03-29 for the daily morning finance/news report: merge the old sections 5 and 6 into one shorter client-facing platform section that lists each platform's hottest discussed product(s) plus the main discussion theme(s); do not mention scraping/fetch limitations in the outward report; in section 2, map the news impact to the user's own product candidate pool rather than generic whole-market products; explicitly list concrete source platforms in the opening; and add a retail-sentiment / 散户端情绪衡量 dimension in the upgraded version.
- The morning finance/news report template was further locked on 2026-03-29: use the tighter client-facing version with (1) an opening “信息来源” line naming concrete platforms, (2) section 2 marking each candidate-pool product as 偏利多 / 偏利空 / 中性偏多 / 中性偏空, (3) a merged concise platform-discussion section, and (4) a short retail-sentiment section. This should be the default every morning unless the user explicitly changes it. Before each send, re-check the template structure so the next day's report does not drift or regress.
- On 2026-04-01, the user refined the morning workflow further: section 2 must choose products only from the latest user-provided candidate-pool Excel; section 4 should be compressed; section 5 should merge same-type platforms (e.g. 东方财富/雪球/同花顺, 富途/老虎 or later replacements, Reuters/CNBC) and describe 散户讨论及舆论主要方向 without platform-trait commentary or per-platform product lists. Also add a 09:25 append-only block every day using Eastmoney-stock on the candidate-pool Excel to list the real-time top 3 movers in the pool as 代码 + 涨跌幅 + 简述原因, plus a short A股/港股开盘概述.
- On 2026-04-01, user requested this 09:25 append workflow be treated as a long-term template/default. User also wants the cross-border discussion-monitor source in section 5 changed from 老虎 to 长桥 if feasible; prioritize Longbridge ahead of Tiger, while keeping the grouped-platform writing style.
- On 2026-04-16, user changed the morning finance/news report send time from 08:30 to 07:30 Beijing time. The format preference was also tightened: section 1 must prioritize genuinely new items from the previous afternoon through overnight instead of stale carry-over topics, and section 5 must name specific platform groups explicitly (for example 东方财富/雪球/同花顺, 长桥/富途, Reuters/CNBC) rather than vague wording like “某平台显示”.
- On 2026-04-19, the user explicitly flagged a real regression in the morning finance/news report: section 1 used stale pre-window news and section 2 used products outside the latest candidate pool. Durable fix rule: before each send, force two explicit checks, (1) every section-1 item must be individually publication-time checked to fall within the Beijing monitoring window, and (2) every section-2 product must be drawn only from the newest inbound candidate-pool Excel (`/root/.openclaw/media/inbound/关注产品---*.xlsx`, newest by mtime, fallback `/root/.openclaw/workspace/data/focus-products.xlsx`). Treat violations of either rule as preventable errors.
- On 2026-04-16, a durable Korean-tech hidden page was established at `https://hketf-lab.pages.dev/insight/korea-tech-briefing.html`. It should remain a clean, client-facing light-theme dashboard around Samsung Electronics and SK Hynix. Current durable logic: Naver polling is the primary real-time quote source, with `m.stock.naver.com/api/stock/{code}/basic` and `/integration` as structured补强/兜底; no visible source/process wording, no visible last-refresh or last-tick wording, no market-status badge; related-product cards must preserve the corrected mapping `07709 = SK海力士每日杠杆 (2x)`, `07747 = 三星每日杠杆 (2x)`, `07347 = 三星每日反向 (-2x)`, and `03431 = 港韩科技`, with the first three tagged `港股账户` and `03431` tagged `即将进入A股港股通/港股账户`; 03431 should route to Eastmoney and use the CTA wording `进入东方财富查看页`.
- On 2026-04-16, a durable Trump-archive page direction was established. `https://hketf-lab.pages.dev/insight/trump-truth-archive.html` is the preferred long-term version over the lighter `trump-pulse` page. It should use `trumpstruth.org` as the archive source, expose our own endpoint `/api/pulse/trump-truth-archive`, keep a light timeline style, default to Chinese with one-click Chinese/original toggle, keep the latest 20 posts, use a fixed Trump avatar, and enrich entries with reaction counts and image/video media when auxiliary feed data is available. `trump-pulse.html` remains acceptable as a lighter alternate feed, but `trump-truth-archive.html` is the more durable own-stack direction. On 2026-04-17 this was tightened further: outward Trump feeds should no longer depend on `stock.fengle.me`; `trump-feed` was rerouted to our own archive path rather than the old fengle-based feed.
- On 2026-04-17, a public stable website baseline was intentionally frozen as `对外稳态版 M1` / `Public Stable Mobile M1`. Current covered pages are homepage, Korea tech page, Trump archive, masters test page, morning report, pool-state, pool overview, and dev index. Durable release rules for this baseline: preserve desktop structure, do mobile optimization mainly through breakpoint-level refinement; keep the 10 real-master portraits on `masters-hk-etf`; for Korea page keep Chinese outward presentation and stable Naver-based sourcing; for `pool-state` keep `每日买入卖出信号` visible and keep the Longbridge fallback sentence hidden; and prefer source-of-truth fixes plus publish, not hand edits that will regress on the next refresh.
- On 2026-04-30, the user explicitly approved `https://hketf-lab.pages.dev/insight/korea-client/` (`韩国科技与市场观察`) as a current finalized outward page and asked for a visible homepage入口 to be added on the main site. Durable rule: preserve this page’s current overall structure/style as the baseline, keep the homepage entry visible unless the user later changes it, and avoid casually reworking the page format from scratch.

## Arena / AI分析师擂台
- On 2026-04-18, the user explicitly elevated `AI分析师擂台` / Arena to a very important long-term commercialization project. Treat it as a durable product line, not a throwaway demo.
- Durable product direction: do not freeze the 10 analysts as static personas. Each analyst should accumulate history, accept feedback, become measurably better over time, and still remain comparable and auditable.
- Durable infrastructure baseline established on 2026-04-18: Arena now runs on a managed Longbridge runtime with fixed CLI path `/root/.openclaw/bin/longbridge`, fixed runtime root `/root/.openclaw/state/longbridge-runtime`, fixed token path under that runtime, wrapper-based auth/check/run scripts, and fallback market-data flow `Longbridge -> Eastmoney -> Tencent` with alert output when all requested providers fail.
- Durable operational rule: Arena should be treated as truly running only when there is a real active background task or a just-finished validated pipeline run. Do not tell the user it is “still running” unless a real session/process confirms that.
- As of 2026-04-19, Arena has a real end-to-end main pipeline that writes market snapshot, features, analyst views, portfolios, leaderboard, eval snapshot, duel snapshot, feedback summary, learning state, growth snapshot, renders pages, and publishes the site.
- Durable reporting rule from 2026-04-18: on every trading day after market close, each analyst should report daily trading results, trading observations, and the next-day planned action; on non-trading days, analysts should keep learning and report what was optimized and where they became smarter.
- Durable learning baseline from 2026-04-18 to 2026-04-19: `data/arena/learning_state.json` and `data/arena/latest/learning_state.json` are the source-of-truth learning artifacts, tracking per-analyst confidence bias, position multiplier, confidence floor, applied step size, mode, reason, and learning history. `run_analysts.py` reads these so learning changes behavior instead of only narration.
- Durable product-state baseline by 2026-04-19 public publish: Arena homepage `site/arena/ai-analysts.html`, growth page `site/arena/growth.html`, and per-analyst detail pages under `site/arena/analysts/` are live on the main site `https://hketf-lab.pages.dev/arena/`. Public-facing product traits now include Zero orchestration weights, growth trajectory display, per-analyst learning/parameter traces, and richer analyst detail pages that explain why an analyst is promoted/suppressed and how it is changing.
- Durable next-step direction for Arena: keep prioritizing the Arena line separately from the morning report line. The user explicitly split these on 2026-04-19 and wants Arena to keep moving toward public-beta readiness and “the world’s best AI analyst arena.”
- On 2026-04-23, a major Arena accounting bug was confirmed and fixed in `scripts/arena/update_portfolios.py`. The old `rebuild_holdings_from_trades()` logic incorrectly handled repeated buys of the same symbol by deducting cash each time while overwriting holdings with only the latest buy, and also treated sells as full symbol removal instead of proper share reduction. This created fake catastrophic drawdowns such as negative dozens of percent even when the held ETF had not moved enough to justify them. Durable rule: treat any Arena performance diagnosis before this fix with caution unless the portfolios were recomputed after the corrected holdings-rebuild logic.
- Also on 2026-04-23, Arena was fully recomputed after the accounting fix, including `market_snapshot`, `features`, `analyst_views`, `leaderboard`, `eval_snapshot`, `duel_snapshot`, `feedback_summary`, `learning_state`, `growth_snapshot`, and page renders. The corrected latest eval state normalized prior impossible returns into plausible values, for example around: dividend_defensive -0.26%, fundamental_value +0.43%, macro_allocation +0.16%, meta_allocator +0.14%, moneyflow -0.85%, sector_rotation +0.20%, sentiment_reversal 0.00%, tape_structure +0.05%, trend_tech +0.38%, volatility_trader +0.78%. Future user-facing comments about current Arena performance should rely on recomputed post-fix artifacts, not older pre-fix snapshots or stale page content.
- On 2026-04-23, Arena analyst convergence around `03454.HK` was confirmed as a real product issue, though less severe after signal adjustments. Durable diagnosis: convergence came from a combination of (1) same-day Zero orchestration favoring `进攻派` / `趋势/资金`, (2) strong momentum/trend features on `03454.HK`, and (3) overlapping analyst universe overrides for multiple attack-style analysts. Future differentiation work should explicitly strengthen per-analyst veto rules, regime fit, and universe separation instead of relying only on light style tags.
- On 2026-04-23, leveraged-product access was deliberately expanded in Arena’s live decision pool by promoting selected products from `experimental` to `expansion` in `data/arena/universe_master.json`, specifically including `07709.HK` (2x SK Hynix), `07747.HK` (2x Samsung Electronics), and `07568.HK` (2x short Nasdaq 100). Durable product direction: leveraged products are allowed and sometimes desirable for the Arena paid-product posture, but their entry should remain style-specific rather than universally forced.
- On 2026-04-23, several analyst universe overrides in `scripts/arena/run_analysts.py` were materially adjusted to reduce crowding into `03454.OF` and to let more style-specific analysts naturally consider leveraged expressions such as `07709 / 07747 / 07226 / 07266 / 07568`. Durable direction: keep increasing analyst differentiation by using stronger universe separation, especially for `moneyflow`, `sector_rotation`, `trend_tech`, `volatility_trader`, and `sentiment_reversal`, while preserving explicit `今天不交易` as a first-class allowed outcome.
- On 2026-04-23, the Arena `daily-full-explainer` page was recognized as stale after the accounting fix and was regenerated/published from corrected data. Durable rule: when Arena portfolio/accounting logic changes materially, regenerate not just leaderboard/eval artifacts but also `daily-full-explainer.html` and related outward pages before discussing current analyst results with the user.
- On 2026-04-20, user clarified commercial posture for Arena: optimize toward a future paid high-value subscription product, not a conservative toy. The user explicitly allows (1) short-term smarter-looking behavior even if volatility is somewhat higher, (2) bold suppression of weak analysts, (3) clear style tilts when evidence supports them, and (4) explicit `今天不交易` outputs. Future Arena design should use these permissions to strengthen differentiation, conviction, and product value while still keeping auditability and risk controls.
- Also on 2026-04-20, Arena crossed an important productization threshold beyond strategy logic alone. Durable additions now include a daily full-market/full-system explainer flow intended for client-facing paid delivery at 21:00 Beijing time after learning/post-close updates, stronger page/report semantics around promoted vs suppressed analysts, no-trade as a first-class decision, regime fit explanations, cash-discipline framing, and clearer NAV labeling. The practical product judgment is that Arena has moved from a strong prototype toward a first subscription-style operating system: product form and readability improved materially, but real statistical credibility is still limited by very short live sample history and should not be overstated.
- Later on 2026-04-20, the user hardened the spec for the 21:00 Arena explainer: it must not be a thin system note. It must always include all major sections at subscription quality, especially (1) Hong Kong market overview, (2) focus product-pool analysis, (3) Zero overall judgment, (4) analyst scoreboard/division of labor, (5) full analyst-by-analyst lower half, (6) system learning, and (7) next-day watchpoints. This explainer now needs a visible entry inside the Arena homepage itself and should update every HK trading day at 21:00 Beijing time. Treat this as a durable Arena product requirement, not a one-off formatting preference.
- On 2026-04-21, CSOP ResearchFlow login/access was successfully established and must be treated as a durable daily-use capability. Critical durable rule: for this site, reusing plain `storage-state.json` alone is not reliable enough because API auth may fail with `Token used from different device`. The preferred long-term method is Playwright with a persistent Chromium profile via `launchPersistentContext`, using profile dir `state/csop-researchflow-profile`, then reusing that same profile for daily runs. Durable scripts: `scripts/csop-researchflow-login.js`, `scripts/csop-researchflow-login-headful.sh`, and most importantly `scripts/csop-researchflow-daily.js` for daily logged-in export. Successful sample daily output path: `state/csop-researchflow-daily-main/result.json`, which captured a real `200` feed response from `/api/research/feed?first=10`. Never store the user's plaintext CSOP password in MEMORY or daily notes; store only the method, paths, and the fact that CSOP is now part of the daily toolkit.
- On 2026-04-22, `CSOP ResearchFlow 每日机构研究摘要` was upgraded from an ad hoc report into a durable nightly product line. Durable product rules: use filter/query-driven daily full scan first, then summarize by category, then choose only the most valuable 3-4 reports, with no empty small categories allowed across 宏观市场类（美国 / 中国 / 日本 / 欧美 / 中国香港 / 韩国）, 多资产分析（股票 / 债券 / 汇率 / 商品）, and 重点行业（科技 / AI / 医药 / 消费）. If filter coverage is weak, auto-fallback to query retrieval. Durable rendering/output paths now include runner `scripts/run_csop_daily_briefing.sh` and public `latest` page `https://hketf-lab.pages.dev/insight/csop-daily-briefing-latest.html`, which successfully ran end-to-end and published for regression date 2026-04-21 with all categories populated. Operational rule agreed with the user: start real nightly runs from tonight at 23:00 Beijing time, and treat early-day sparse data as a validation caveat rather than a product-definition failure.
- On 2026-04-23, a real operational gap was confirmed in the CSOP nightly chain: the runner script existed, but the 23:00 Beijing-time cron had not actually been created. Durable fix: a silent cron job named `CSOP daily briefing latest 2300` was added directly in `/root/.openclaw/cron/jobs.json` with daily `0 23 * * *`, `Asia/Shanghai`, `delivery.mode = none`, and payload to run `bash /root/.openclaw/workspace/scripts/run_csop_daily_briefing.sh`. Future recurring-work claims for CSOP should be verified against real cron state, not memory alone.
- Also on 2026-04-23, the CSOP `latest` page needed a date correction after a manual rerun defaulted to the current Beijing date instead of the intended previous-night report date. Durable rule: when backfilling or correcting `latest`, explicitly pass `CSOP_REPORT_DATE` so the outward `date` matches the intended report session. The validated corrected latest state for that repair cycle was `date = 2026-04-22` with fresh generation timestamp `2026-04-23T02:45:47.135Z`.
- Also on 2026-04-23, the user tightened the outward product standard for CSOP deep reads. Durable page rule: the `精读` section must look more professional and commercial, remove internal wording like `隐藏页版本`, avoid repeated selection of the same report across multiple deep-read slots, and prioritize real chart/image-bearing reports whenever possible. This should be solved in the generation/render logic rather than by hand-editing published HTML.
- Also on 2026-04-23, the user defined a stronger lasting CSOP quality floor: future nightly `csop-daily-briefing-latest` reports should at least preserve the improved 2026-04-22 session quality reached during this repair cycle. Practical durable meaning: keep three deep reads split across the three big buckets, keep real images when available, keep the more client-facing layout, and do not regress to sparse/草率 deep-read presentation. If concise summarization quality is still weak, prefer retaining more useful full-detail content from the report-detail page over aggressively compressing the report into shallow pseudo-summaries.
- Also on 2026-04-23, the user made the CSOP category-card standard more explicit: each small category `主判断` line must be a real synthesized judgment with useful directional content, based on the reports/details read in that category, not empty meta commentary or category-description filler. Treat weak “这组更偏…” style wording as a quality failure even if the category selection itself is roughly correct.
- On 2026-04-24, the user further hardened the CSOP nightly standard: the current generated `csop-daily-briefing-latest` output quality, including de-duplicated category picks, stronger client-facing `主判断`, fuller top-of-page overview, cleaner public-facing page language, and improved deep-read image presentation, should be preserved in long-term memory as the minimum acceptable baseline for every future 23:00 Beijing-time refresh. Do not let nightly runs regress below this quality bar.
- On 2026-04-27, the user hardened the CSOP scheduling rule further: future nightly CSOP fetching/generation should happen only on trading-day evenings. Non-trading evenings must skip the fetch and keep the previous valid trading-day `latest` page, rather than refreshing to empty/sparse/non-trading output.
- Also on 2026-04-23, the CSOP nightly trigger was re-verified after the earlier miss: job `CSOP daily briefing latest 2300` is currently present and enabled in `/root/.openclaw/cron/jobs.json` with schedule `0 23 * * *` under `Asia/Shanghai`, isolated execution, and silent delivery. Durable ops rule: for CSOP nightly continuity, keep checking the real scheduler state instead of trusting chat memory alone.
- On 2026-04-22, a hidden Polymarket monitoring page direction was established at `https://hketf-lab.pages.dev/insight/alpha-signal-briefing`. The original snapshot-refresh path (`scripts/build_polymarket_radar.py` + `scripts/run_polymarket_radar_refresh.sh`) exists as legacy scaffolding, but the durable outward product direction was later upgraded the same day to the API-driven path centered on `functions/api/pulse/polymarket-radar.js` plus `site/insight/alpha-signal-briefing.html`. Keep the page hidden with no homepage entry, and continue hard-excluding any Xi Jinping / 习近平 / Taiwan / 台湾 / 台海 related predictions or content from both the page and generated data.
- Durable Polymarket product rules established on 2026-04-22 after iterative cleanup: (1) outward pages must not show internal-progress wording such as `隐藏测试页`, `候选骨架`, `下一步继续补强`, or similar dev-state narration; (2) `1 小时异动` is the primary top-of-page entry and must never appear as a dead empty block, falling back to a `最近 1 小时最活跃 / 最值得看` candidate view when the strict baseline detector has no mature hits; (3) anomaly baseline caches must not be overwritten on every request, otherwise 1-hour deltas collapse toward fake zeros by comparing the current snapshot against itself; (4) front-end layout simplification requires removing stale JS render bindings for deleted DOM blocks, or later sections such as smart-money and market lists can silently stop rendering.
- Durable Polymarket data-quality rule from 2026-04-22: many Gamma markets do not reliably provide `oneHourPriceChange`, `oneDayPriceChange`, or `oneWeekPriceChange`. Missing change fields must never be outwardly coerced to `0%`; show explicit missing-state UI like `--`, and only show percentage moves when upstream data exists or a clearly defined fallback has been computed.
- Durable Polymarket feature baseline by 2026-04-22 end-of-session: the API-driven page now includes 1-hour anomaly detection, resonance labeling (`单点观察` / `双重共振` / `三重共振`), smart-money seed tracking sourced from `polymarketanalytics.com/traders` plus Polymarket Data API (`positions` / `value` / `trades`), theme-level aggregation of smart-money flows, representative market linkage under each theme, a top-line `今日总判断`, and a `今日最该盯的 3 个市场` layer. Future related requests should treat these as the current product baseline rather than the earlier plain market list.

## Trading research preferences
- For the ongoing 7-product Hong Kong trading research, the user wants a trader mindset rather than passive product summaries: the goal is to learn how to actually trade this product category, not merely describe it.
- On 2026-04-11, a new long-term indicator/workflow was established for the 59-product focus pool: call it the “乖离率指标”. The intended logic is based on Liu Chenming-style LOGBIAS, i.e. using log-price relative to EMA20 of log-price to classify single-product state, strength, repair, and pressure zones. This is primarily a single-product timing/state indicator that can be batch-run across the whole pool, not a pool-level aggregate indicator. Default visualization should use product-level status + structure + recent 10-day trajectory, and when Longbridge daily kline lags the latest trading day, patch the last day via Longbridge intraday before recomputing. Future related requests should recognize “乖离率指标” as this long-term framework.
- Also on 2026-04-11, the user decided to narrow scope after the pool-wide visualization: instead of continuing broad pool research, prioritize deep single-name work on `03033` (恒生科技ETF), especially backtesting a LOGBIAS-based momentum/timing strategy over roughly the past three years and comparing it against simple buy-and-hold.
- For `03033`, the preferred “formal” simulation convention is now: observe the LOGBIAS signal at today’s close, then execute at the next trading day’s open. Within that convention, the current balanced right-side LOGBIAS rule of record is `buy when LOGBIAS >= 3, sell when LOGBIAS <= -1`, while a separate left-side mean-reversion branch should also be explored using low-bias buys and high-bias sells.
- This research line should continuously optimize and continuously write memory: preserve evolving conclusions about execution, liquidity, spread/slippage, structural risk, strategy fit, and practical do-not-trade rules instead of relying on transient session context.
- For this line, Tencent Finance should be the primary market-data source by default; Eastmoney is downgraded to backup/cross-check because it is more prone to blocking and should not be the main dependency.
- The user signaled that these 7 products may eventually be used for real trading, so research should be framed around live execution reality for a 100,000 HKD account.
- New emphasis on 2026-03-29: once longer extended/proxy sequences are available, do not stay locked on only 03033 + 03441. Re-test whether other products from the original 7-name pool can be added back in on longer-history evidence, and explicitly study timing-aware structures with cash, lower exposure, and flexible position sizing; do not assume fixed/full exposure because the user explicitly allows very flexible allocation.
- Additional refinement on 2026-03-29: the time-extension / proxy-reconstruction approach used for 03441 should be generalized to the other products when feasible. The user wants systematic attempts to lengthen or proxy-reconstruct timelines across the 7-name pool, not just for one or two flagship products, to see whether short ETF samples are hiding useful structure.
- On 2026-04-12, the user fixed the operating mode for the 59-product batch backtest: use forward-adjusted prices by default, deliver results in 10-product batches without waiting for reconfirmation after each batch, and persist the full-pool per-product results into long-term memory as the future charting / overview / case-page source of truth.
- The per-product result schema the user wants preserved is: time range; left-side best parameters / return / trade count; buy-and-hold return; right-side best parameters / return / trade count; and a short conclusion.
- Also on 2026-04-12, the source-of-truth full-pool run was upgraded from integer thresholds to a denser 0.5-step LOGBIAS grid while keeping practical filters (left min spread 6, right min spread 4, prefer trades>=2). This denser scan materially changed some conclusions, so future comparisons should treat `output/pool_logbias_compare_dense/` plus `memory/2026-04-12-logbias-full-pool.md` as the canonical full-pool baseline rather than the earlier coarse-grid export.
- Also on 2026-04-12, for the 59-product client deliverable, the user explicitly preferred the previously approved mature overview image format over any redesign: keep the old box/card heights and overall layout style, and only replace the latest data plus the opening/closing copy. The current archived web baseline is `output/pool_logbias_compare_overview_v9_exact/index.html`.
- For the PNG image line, the latest acceptable direction is the “motherstyle_loose” variant, which enlarges the outer cards, curve area, and the three text boxes below each curve. Current archived image baseline: `output/pool_logbias_compare_overview_v9_exact/overview_v9_exact_motherstyle_loose_20260412T155701Z.png`.
