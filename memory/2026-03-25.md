# 2026-03-25

- User prefers a reliable buddy vibe (靠谱搭子型).
- User wants us to address each other as “bro”.
- Installed and validated workspace skill `daily-stock-analysis` (Ready).
- Installed and validated workspace skill `eastmoney-stock` (Ready).
- Installed and validated workspace skill `qveris` (Ready, but requires `QVERIS_API_KEY` for real use).
- Installed workspace skill `tushare`; it is not ready until `TUSHARE_TOKEN` is configured.
- Installed and repaired Eastmoney 妙想 managed skills `mx-data`, `mx-search`, `mx-select-stock`, `mx-selfselect`, and `mx-stock-simulator`; configured `MX_APIKEY` for them in `~/.openclaw/openclaw.json` and verified they are recognized as Ready.
