# 2026-03-26

- User confirmed the assistant should be a reliable buddy (靠谱搭子) and also be called “bro”.
- Installed Playwright Chromium runtime dependencies successfully on the host; browser can now launch.
- Investigated scraping Futu 07709 community. Direct access to https://www.futunn.com/hk/etfs/07709-HK/community redirects to passport.futunn.com and requires login.
- Yesterday's raw artifact for 07709 community shows comments/replies were previously captured successfully via credential-based automation, but the same flow now fails.
- Multiple login attempts were tested today: original last-night script, generic passport login flow, forced DOM/login-state switching, plain-login-then-search flow, and a stealthier persistent-profile flow.
- Findings indicate the login page can sometimes be made to show the email/account login UI, but submission does not reach the password step in the current environment.
- Traced request flow shows a critical failure mode: clicking "下一步" in the apparent account-login UI still triggers POST https://passport.futunn.com/reg/send-sms, sending the email in phone_no and returning {"code":6001,"message":"參數錯誤"}. This means the visible login UI can still be backed by a registration controller/state.
- Another test with lower automation fingerprint and persistent profile improved surface behavior (page title stable as "登入"), but still did not advance to a password input.
- Current best recommendation to the user is to reuse a real logged-in Futu session (cookies/storageState or full browser profile/user-data-dir) and then let Playwright access 07709 community with that login state.
- User is open to a login-state reuse approach and was asked to choose among cookies, full browser profile, or instructions for exporting cookies.
- For video comment OCR tasks, user wants comments + replies only (no usernames or other metadata), and explicitly prioritizes completeness over accuracy: “可以错，但要都有。” Output should have two layers: a full rough parse first, then a refined original-quote selection.
